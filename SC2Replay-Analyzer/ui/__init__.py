"""UI layer (customtkinter).

`app.App` is the main window. `visualizer.GameVisualizerWindow` is the
per-game graph window. `theme` holds the color/font constants and the
`wr_color` helper used everywhere for win-rate coloring.
"""
