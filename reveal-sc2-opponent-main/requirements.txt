# SC2 Tools (Merged Toolkit) -- Python dependencies
# Install with:  pip install -r requirements.txt

# ---- Replay parsing (core engine + watchers + analyzer) -----------
sc2reader

# ---- Live replay watcher ------------------------------------------
# Watches the SC2 replay folder and POSTs to the overlay backend.
watchdog
requests

# ---- MMR OCR scanner ----------------------------------------------
# NOTE: pytesseract requires the Tesseract binary on disk.
# Download Tesseract from: https://github.com/UB-Mannheim/tesseract/wiki
mss
opencv-python
numpy
pytesseract

# ---- Analyzer GUI -------------------------------------------------
# Customtkinter is the desktop UI, matplotlib drives the in-app graphs.
customtkinter
matplotlib
