# SC2 Tools (Merged Toolkit) -- Python dependencies (pinned).
#
# Pin policy: every dependency uses == exact versions. See
# SC2Replay-Analyzer/requirements.txt for the regeneration playbook.
# Versions current as of 2026-04 against Python 3.12.

# ---- Replay parsing (core engine + watchers + analyzer) -----------
sc2reader==1.8.0

# ---- Live replay watcher ------------------------------------------
# Watches the SC2 replay folder and POSTs to the overlay backend.
watchdog==5.0.3
requests==2.32.3

# ---- Numerics (replay analytics) ----------------------------------
numpy==2.1.3

# ---- Analyzer GUI -------------------------------------------------
# customtkinter is the desktop UI, matplotlib drives the in-app graphs.
customtkinter==5.2.2
matplotlib==3.9.2
