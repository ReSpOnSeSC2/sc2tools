requests==2.32.3
sc2reader==1.8.0
watchdog==5.0.3
pystray==0.19.5
Pillow==11.0.0
python-dotenv==1.0.1
# python-socketio client lets the agent listen for live recompute
# requests from the cloud (e.g. the SPA's "Recompute" button on the
# macro breakdown sheet). Keeps the agent's connection footprint low
# — one long-lived ws to the API, multiplexed with the existing
# device-token auth.
python-socketio[asyncio_client]==5.11.4
# Crash reporter - optional at install time (the agent degrades to
# console logging when the dep is absent), pinned here for reproducible
# .exe builds.
sentry-sdk==2.18.0
# PySide6 powers the production GUI (sc2tools_agent.ui.gui). The agent
# falls back to the system tray + console UI when this is missing, so
# the dependency is recommended-but-not-required for source installs.
# Loose pin (>=6.7,<7) so pip can pick whichever 6.x wheel matches the
# Python build on the runner — strict ==6.8.0.1 has bitten us when the
# matching wheel isn't published for a 3.12 sub-version yet.
PySide6>=6.7,<7
